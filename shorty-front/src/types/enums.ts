// enums for status
export enum UrlFormStatus {
  idle = "idle",
  loading = "loading",
  completed = "completed",
  error = "error",
}

// Button categories for Button component (used in styles)
export enum ButtonCategory {
  primary = "primary",
  secondary = "secondary",
}
